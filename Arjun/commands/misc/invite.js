/*
const { MessageEmbed } = require('discord.js');
const handler = require('../../handlers/message');
module.exports = {
    name: 'invite',
    aliases: ['inv'],
    usage: 'invite',
    description: 'Give you a link to invite this bot',
    async execute(message, args, client) {
        message.channel.send(new MessageEmbed().setColor(client.colors.main).setDescription(`# **[Click here](https://discord.com/api/oauth2/authorize?client_id=1203646619522834452&permissions=36768832&scope=bot)**`)
    //return message.channel.send(embed)
      .setAuthor(`Invite Panel`, client.user.displayAvatarURL()))
    }
    
}
*/
